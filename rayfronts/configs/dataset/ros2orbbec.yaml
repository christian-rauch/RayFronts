defaults:
  - base_dataset

_target_: rayfronts.datasets.Ros2Subscriber

rgb_topic: null

rgb_compressed_topic: "/camera/color/image_raw/compressed"
depth_compressed_topic: "/camera/depth/image_raw/compressed"
intrinsics_topic: "/camera/color/camera_info"
pose_topic: "/camera_pose"

# A string of 3 letters describing the camera coordinate system in
# r/l u/d f/b in any order. (e.g, rdf, flu, rfu)
src_coord_system: "rdf"
